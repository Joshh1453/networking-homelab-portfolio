# Documentation-only WireGuard client template
# Replace every placeholder before use. Never commit a real private key.

[Interface]
PrivateKey = <CLIENT_PRIVATE_KEY>
Address = 10.50.70.2/24
DNS = 10.50.10.1

[Peer]
PublicKey = <FIREWALL_PUBLIC_KEY>
Endpoint = vpn.example.net:51820
AllowedIPs = 10.50.10.0/24, 10.50.20.0/24, 10.50.60.0/24
PersistentKeepalive = 25

# Route only the networks required for administration.
# Do not use this template as a production configuration without
# reviewing firewall policy, DNS, endpoint security, and key storage.